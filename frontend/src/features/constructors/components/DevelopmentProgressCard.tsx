import PlaceholderPanel from "@/features/shared/components/ui/PlaceholderPanel";

export default function DevelopmentProgressCard() {
  return (
    <PlaceholderPanel
      title="Development Progress"
      subtitle="Active aerodynamic workstreams"
      description="Upgrade tracking isn't sourced from OpenF1 - no data available."
      fill={false}
    />
  );
}
