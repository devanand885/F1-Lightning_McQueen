import PlaceholderPanel from "@/features/shared/components/ui/PlaceholderPanel";

export default function PerformanceProfileRadar() {
  return (
    <PlaceholderPanel
      title="Performance Profile vs Grid Mean"
      subtitle="Normalized technical capability index"
      description="Needs a defined car-performance model - part of the upcoming analytical layer."
      className="min-h-[410px]"
    />
  );
}
